version: 2.1

jobs:
  build:
    docker:
      - image: node:latest
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Check linter
          command: npm run lint
      - run:
          name: Run tests
          command: npm test
